// Insert the partial at the bottom of the div list
$('theConversation').insert( { bottom: "<%= raw(patient_response(@userStatement)) %>" } ); 
$('theConversation').insert( { bottom: "<%= raw(eliza_response(@elizaResponse)) %>" } ); 

//$('theConversation').insert( { bottom: "<%= raw(@elizaResponse) %>" } ); 

// Element.hide('processing-indicator');
$('form-submit-button').disabled = false;	
inputText = $('inputTextBox');
inputText.value = '';
inputText.focus();




